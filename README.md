[README.md](https://github.com/user-attachments/files/23693643/README.md)
# PEREEZD.AI – нейросеть для контроля ж/д переездов

Интеллектуальный модуль видеоаналитики для железнодорожных переездов.  
Система в реальном времени анализирует видеопоток с камеры, отслеживает объекты (машины, людей, поезда, шлагбаумы) и формирует триггерные события для оператора и базы данных.

---

## Основные возможности

- 🔍 Детекция объектов на переезде:
  - `barrier_down`, `barrier_up`
  - `rail_zone`
  - `car`
  - `train`
  - `obstacles`
  - `person`
- 🎯 Отслеживание объектов во времени (трекер)  
  и вычисление времени нахождения в зоне ж/д путей.
- ⚠️ Триггерные ситуации:
  - автомобиль/препятствие долго находится на путях;
  - человек на путях при наличии поезда;
  - поезд на переезде при поднятых шлагбаумах;
  - шлагбаум долго опущен без поезда.
- 🧠 Две нейросетевые модели:
  - **Кастомная YOLOv5** для объектов переезда;
  - **Стандартная YOLOv5s** только для людей (запускается реже для экономии ресурсов).
- 📄 Логирование событий:
  - файл `events_log.jsonl` (по одному JSON на строке);
  - таблица `crossing_events` в PostgreSQL.
- 🖥 Интеграция с десктоп-клиентом оператора  
  (PyQt-приложение читает события и показывает триггерные окна).

---

## Архитектура

```text
Камера → mini-PC (Python, YOLOv5) → JSON-события → PostgreSQL / файл → Desktop-клиент оператора
```

На mini-PC:

- `main.py` – точка входа, запускает `CrossingDetector`;
- `detector.py` – работа с видео, нейросетями, логика триггеров, запись событий;
- `tracker.py` – простой трекер объектов (ID, сглаживание, подсчёт времени в зоне);
- `triggers.py` – описание классов детекций и геометрия зоны переезда;
- `config.py` – параметры работы (разрешение, пороги, интервалы, настройки БД);
- `event_db_pg.py` – запись событий в PostgreSQL;
- `events_log.jsonl` – резервный лог в формате JSONL.

---

## Технологии

- **Python 3.11**
- **PyTorch + YOLOv5**
  - кастомная модель: `best_barrier_rail_v1_*.pt`
  - стандартная модель людей: `yolov5s.pt`
- **OpenCV** – работа с видеопотоком и отрисовка;
- **pandas** – удобная работа с результатами детекции;
- **PostgreSQL 18 + psycopg2** – хранение событий (`crossing_events`);
- **JSONL** – быстрый лог событий для интеграции и отладки.

---

## Логика работы детектора

1. Открываем источник:
   - локальная камера (`0`, `1`, …) или
   - RTSP-поток / видеофайл.
2. Каждый кадр:
   - масштабируется до целевой ширины (`IMG_TARGET_WIDTH`);
   - подаётся в кастомную YOLOv5 → получаем объекты переезда;
   - раз в `PERSON_INTERVAL_SEC` секунд кадр подаётся в `yolov5s` только для людей.
3. Все детекции объединяются, из `rail_zone` строится зона путей.
4. `SimpleTracker`:
   - присваивает объектам стабильные `track_id`;
   - сглаживает координаты;
   - считает время нахождения объекта в зоне (`DWELL_SECONDS`);
   - генерирует события `object_dwell_in_crossing`.
5. Дополнительные триггеры по кадру:
   - человек + поезд в зоне;
   - поезд при поднятых шлагбаумах;
   - шлагбаум долго опущен без поезда.
6. Для каждого события формируется JSON:

   ```json
   {
     "event_id": "...",
     "type": "car_dwell_on_tracks",
     "source": "crossing_ai",
     "camera_id": "crossing_1",
     "timestamp": "2025-11-22T15:30:12.123456",
     "track_id": 7,
     "object_class": "car",
     "dwell_time_sec": 5.4,
     "plate": null,
     "description": "Автомобиль находится в зоне ж/д путей более 5.4 секунд"
   }
   ```

7. JSON записывается:
   - строкой в `events_log.jsonl`;
   - в PostgreSQL в таблицу `crossing_events`.

---

## Установка и запуск

### 1. Клонирование репозитория и окружение

```bash
git clone <url_репозитория> PEREEZD.AI
cd PEREEZD.AI

python -m venv env311
.\env311\Scripts\Activate.ps1   # Windows PowerShell
pip install -r requirements.txt
```

### 2. Настройка `config.py`

В `config.py` задать основные параметры:

- Видеопоток:

  ```python
  CAMERA_SOURCE = 0  # или RTSP-строка / путь к видео
  ```

- Параметры детекции / трекинга:

  ```python
  IMG_TARGET_WIDTH = 640
  FRAME_SKIP = 1
  CUSTOM_CONF = 0.45
  PEOPLE_CONF = 0.5
  DWELL_SECONDS = 3.0
  ```

- Подключение к PostgreSQL:

  ```python
  PG_HOST = "localhost"
  PG_PORT = 5432
  PG_DB   = "pereezd_ai"
  PG_USER = "postgres"
  PG_PASS = "your_password"
  PG_TABLE = "crossing_events"
  ```

### 3. Запуск детектора

```bash
python main.py
```

- В окне OpenCV отображается видеопоток с разметкой.
- При срабатывании триггеров в консоль выводится JSON, а события пишутся в файл и БД.  
- Выход – клавиша `ESC` в окне видео.

---

## Структура проекта (упрощённо)

```text
PEREEZD.AI/
├─ yolov5/                    # локальный репозиторий YOLOv5
├─ datasets/                  # исходные датасеты для обучения
├─ dataset_merge/             # объединённый датасет (рельсы, барьеры, поезда, машины)
├─ best_barrier_rail_v1.pt    # обученная кастомная модель
├─ main.py                    # точка входа
├─ detector.py                # логика детекции, триггеров и логирования
├─ tracker.py                 # простой трекер
├─ triggers.py                # описание Detection и зоны CROSSING_AREA
├─ config.py                  # настройки (нейросеть, трекер, БД, камера)
├─ event_db_pg.py             # работа с PostgreSQL
├─ events_log.jsonl           # журнал событий (JSONL)
└─ README.md                  # (этот файл)
```

---

## Интеграция с клиентом оператора

- Нейросеть **ничего не знает** о UI оператора — она только генерирует события.
- Десктопное приложение (PyQt):
  - читает таблицу `crossing_events` в PostgreSQL;
  - показывает триггерные окна («Неисправность оборудования», «Машина на путях при опущенных шлагбаумах» и т.д.);
  - позволяет оператору формировать отчёты и обратную связь по работе нейросети.

---

## План развития

- Добавление отдельной модели распознавания номерных знаков.
- Экспорт модели в ONNX / OpenVINO для работы на более слабых мини-ПК.
- Веб-панель для мониторинга статуса всех переездов.
- Тонкая настройка триггеров под реальные регламенты РЖД / заказчика.
- Система самооценки: оператор оценивает качество срабатывания, данные идут на дообучение.

---

## Контакты

*Здесь можно указать компанию, команду, почту или Telegram для связи.*

```txt
PEREEZD.AI – нейроассистент оператора ж/д переездов
```
